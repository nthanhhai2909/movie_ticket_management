export const requiredValidator = (req, res, next) => {
    // TODO 
    next();
};

export const formatValidator = (req, res, next) => {
    // TODO 
    next();
};


export const consistentValidator = (req, res, next) => {
    // TODO 
    next();
};